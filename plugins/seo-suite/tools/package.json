{
  "name": "seo-audit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canlı ve çevrimdışı çalışabilen, Türkçe raporlayan SEO denetim aracı",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "seo-audit": "./seo-audit.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "audit": "node ./seo-audit.mjs",
    "build:panel": "node ./build-panel.mjs",
    "check": "npm run build:panel && npm test"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright-core": "^1.62.1"
  }
}
